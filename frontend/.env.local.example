BACKEND_URL="http://localhost:3000"
DEFAULT_TENANT_SLUG="demo"
# El navegador se conecta acá DIRECTO (WebSocket, no pasa por el proxy /api/backend) para
# el chat en vivo del watch party. En producción debe ser el origen público del backend.
NEXT_PUBLIC_BACKEND_URL="http://localhost:3000"
