// Categorías sugeridas para canales, películas y series. No son un enum cerrado en el
// backend (category es texto libre) — son solo las opciones que se sugieren en el
// admin y los filtros de /canales, /peliculas, /series y las páginas de categoría.
export const CONTENT_CATEGORIES = ['Deportes', 'Noticias', 'Música', 'Kids', 'Entretenimiento'] as const;
